import { TodoDirectiveDirective } from './todo-directive.directive';

describe('TodoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
